Project Goal: Writing analytical queries directly against the database to generate intelligence insights based on the data processed in Part A.  

To run the project, open a terminal in the root folder of the project and run the following command:
docker compose -f docker-compose.db.yml up -d --build

Then open a browser at the following link:

http://localhost:8000/docs